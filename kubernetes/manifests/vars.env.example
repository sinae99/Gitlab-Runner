# Copy to vars.env and fill in secrets before deploy:
#   cp vars.env.example vars.env

GITLAB_URL=https://gitlab.example.com
REGISTRATION_TOKEN=glrt-xxxxxxxxxxxxxxxxxxxx
RUNNER_NAME=k3s-gitlab-runner
RUNNER_TAGS=k3s,kaniko,docker
JOB_IMAGE=alpine:3.20
JOB_PRIVILEGED=false
JOB_SERVICE_ACCOUNT=gitlab-runner-jobs
